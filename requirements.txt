# BLE Scanner for nRF52DK - Windows Dependencies
# Optimized for c:/Battery-Scanner-Mini-White deployment

# Nordic Semiconductor Python driver (CRITICAL)
# Compatible with Python 3.7+ on Windows
pc-ble-driver-py>=0.17.0

# Data processing libraries
pandas>=1.5.0
numpy>=1.21.0

# Built-in modules (no installation required):
# - logging, datetime, pathlib, json, csv
# - typing, os, sys, signal, threading
